import { Inter, Lusitana, Recursive, Roboto } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({weight: "400", subsets: ['latin']})
export const recursive = Recursive({ subsets: ['latin'] });
export const robotoBold = Roboto({weight:"500", subsets: ['latin'] });
export const roboto = Roboto({weight:"400", subsets: ['latin'] });